<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="50dp"
    android:background="@color/dblue"
    tools:context=".ui.login.LoginActivity">


    <ImageView
        android:id="@+id/ivLogo"
        android:layout_width="0dp"
        android:layout_height="200dp"
        android:layout_marginTop="50dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/chikara_logo_big" />


    <EditText
        android:id="@+id/etUserName"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:layout_marginTop="50dp"
        android:hint="User name"
        android:imeOptions="actionNext"
        android:drawableRight="@drawable/ic_user_login"
        android:backgroundTint="@color/swwhite"
        android:textColorHint="@color/swwhite"
        android:inputType="text"
        app:layout_constraintTop_toBottomOf="@+id/ivLogo"
        tools:layout_editor_absoluteX="10dp" />


    <EditText
        android:id="@+id/etPassword"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:layout_marginTop="20dp"
        android:imeOptions="actionDone"
        android:textColorHint="@color/swwhite"
        android:backgroundTint="@color/swwhite"
        android:drawableRight="@drawable/ic_password_login"
        android:hint="Password"
        android:inputType="textPassword"
        app:layout_constraintTop_toBottomOf="@+id/etUserName" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="50dp"
        app:layout_constraintTop_toBottomOf="@id/etPassword"
        android:layout_marginTop="30dp"
        android:text="Login"
        android:textSize="20sp"
        android:textColor="@color/swwhite"
        android:backgroundTint="@color/psgreen"
        android:id="@+id/btnLogin"
        android:layout_marginBottom="20dp"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="50dp"
        app:layout_constraintTop_toBottomOf="@id/btnLogin"
        android:orientation="horizontal"
        android:layout_marginTop="20dp">
        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Don't have an account?"
            android:textColor="@color/swwhite"
            android:textSize="15sp"/>
        <Button
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="0.5"
            android:text="Register"
            android:textColor="@color/swwhite"
            android:backgroundTint="@color/ssblue"
            android:id="@+id/btnRegister"
            />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>